{
	"name": "Shiny Server",
    "build": {
        "dockerfile": "Dockerfile",
        "args": {
            "VARIANT": "latest"
        }
    },
    "forwardPorts": [
        3838
    ],
    "portsAttributes": {
        "3838":{
            "label": "Shiny Server",
            "onAutoForward": "notify"
        }
    },
    "workspaceMount": "source=${localWorkspaceFolder}/sub-folder,target=/shiny-app,type=bind,consistency=cached", 
    "workspaceFolder": "/shiny-app"
}
